$schema: 'https://moonrepo.dev/schemas/project.json'

tasks:
  lint:
    command: bash
    args:
      - '-c'
      - 'cd $MOON_WORKSPACE_ROOT && find linting/docker -maxdepth 1 -type f ! -name "moon.yml" ! -name ".*" | xargs hadolint'
    inputs:
      - '**'
